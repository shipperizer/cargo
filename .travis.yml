language: python
python:
  - "3.2"
  - "3.3"
  - "3.4"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: "py.test test --cov cargo --cov-config .coveragerc --cov-report xml --junit-xml=results.xml"
os:
 - osx
 - linux
notifications:
   email: false
